.well
  .song-actions
    = link_to new_song_path, class: "pull-right btn btn-large btn-primary" do
      %i.icon-plus.icon-white
      New Song

  .songs-list.clear
    - if @songs.present?
      - @songs.each do |song|
        = link_to song do
          = render song
    - else
      %h2 You don't have any songs yet :(
